# Database
# Required: PostgreSQL connection string (e.g., from Neon)
DATABASE_URL="postgresql://user:password@host:port/database"

# Security
# Required: Generate a strong random secret for session encryption
# Use: openssl rand -base64 32
SESSION_SECRET="your-session-secret-here"

# Required: Admin password for accessing the admin panel
# Can be plaintext or bcrypt hashed (starting with $2)
# To hash a password: npx -y bcrypt-cli hash "your-password"
ADMIN_PASSWORD="your-admin-password-here"

# AI Services (optional)
# OpenAI-compatible API for chat functionality
AI_INTEGRATIONS_OPENAI_BASE_URL="https://your-openai-base-url"
AI_INTEGRATIONS_OPENAI_API_KEY="your-openai-api-key"

# ElevenLabs for text-to-speech
ELEVENLABS_API_KEY="your-elevenlabs-api-key"
ELEVENLABS_VOICE_ID="your-voice-id"

# Server
PORT=5000
NODE_ENV=development
